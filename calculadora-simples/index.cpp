#include <iostream>
using namespace std;

int main() {
    int a, b, s;

    cout << "Me informe um número para a soma: ";
    cin >> a;

    cout << "Me informe outro número para a soma: ";
    cin >> b;
    s = a + b;

    cout << "A soma dos números informados: " << s;
    return 0;
}